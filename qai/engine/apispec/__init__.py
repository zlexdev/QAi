"""apispec — OpenAPI/GraphQL spec parsing, converted to the existing FormModel/
FieldModel vocabulary so the fuzzer/analyzer/reporter run unmodified against
spec-driven API operations. See `_MODULE.md`."""

from __future__ import annotations
